<UserControl
    x:Name="ThisControl"
    x:Class="Reitit.LabelPushpin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Reitit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Polygon Points="0,0 18,0 18,26 0,44" Fill="{Binding Background, ElementName=ThisControl}"/>
        <Border Height="26"
                VerticalAlignment="Top"
                Background="{Binding Background, ElementName=ThisControl}">
            <TextBlock Foreground="White"
                       Text="{Binding Label, ElementName=ThisControl}"
                       FontSize="18"
                       Margin="3,0,3,0"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
