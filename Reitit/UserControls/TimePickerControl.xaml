<me:DateTimePickerControl
    Name="ThisControl"
    x:Class="Reitit.TimePickerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:me="clr-namespace:Reitit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}">

    <me:DateTimePickerControl.Popup>
        <me:TimePickerPopup/>
    </me:DateTimePickerControl.Popup>

    <Button Content="{Binding Value, ElementName=ThisControl, Converter={StaticResource TimePickerConverter}}"
            Background="Transparent"
            Height="72"
            HorizontalContentAlignment="Left"
            Click="Button_Click"/>

</me:DateTimePickerControl>