<UserControl x:Class="Reitit.StatusOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}">

    <Grid Name="Root">
        <StackPanel VerticalAlignment="Center">
            <ProgressBar IsIndeterminate="False" Margin="0"/>
            <TextBlock Name="Status" TextWrapping="Wrap" Style="{StaticResource PhoneTextNormalStyle}" TextAlignment="Center" Margin="{StaticResource PhoneVerticalMargin}"/>
        </StackPanel>
    </Grid>
</UserControl>