<me:LocationPickerControlBase
    Name="ThisControl"
    x:Class="Reitit.TimePickerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:me="clr-namespace:Reitit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}">

    <me:LocationPickerControlBase.Popup>
        <me:LocationPickerPopup/>
    </me:LocationPickerControlBase.Popup>

    <Button Content="{Binding Value, ElementName=ThisControl, Converter={StaticResource LocationPickerConverter}}"
            Background="Transparent"
            Height="72"
            HorizontalContentAlignment="Left"
            Click="Button_Click"/>

</me:LocationPickerControlBase>